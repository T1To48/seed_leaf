import styled from "styled-components";

export const StyledLinksWrapper = styled.div`
  width: 90%;
  & > :last-child {
    border-bottom: none;
  & > :last-child{
      visibility: hidden;
    }
  }
`;
